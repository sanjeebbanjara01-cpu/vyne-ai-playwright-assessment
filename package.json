{
  "name": "vyne-ai-playwright-assessment",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright TypeScript framework for SauceDemo UI and JSONPlaceholder API testing",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:ui:chromium": "playwright test --ui --project=ui-chromium",
    "test:api": "playwright test --project=api",
    "test:api:ui": "playwright test --ui --project=api",
    "test:headed": "playwright test --headed --project=ui-chromium",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}